# Electron app always uses https://api.sl1de.xyz (hardcoded, no override)

# CDN base URL for static files (uploads, avatars)
# When set, static assets load from CDN instead of backend
# SLIDE_CDN_BASE_URL=https://cdn.example.com
